# Bugs — sibur-reestr-bot

Лог багов и подозрительных мест, найденных по ходу другой работы.
Правило: если баг **не относится** к текущей задаче — записать сюда и продолжить, не теряя контекст. Не лезть в фикс «по дороге».

---

## 🔴 Открытые

<!-- Шаблон записи:
### [YYYY-MM-DD] Короткое название
- **Файл:** `path/to/file.py:LINE`
- **Симптом:** что не так / как воспроизвести
- **Серьёзность:** low | medium | high | critical
- **Найдено в задаче:** что делал, когда заметил
- **Идея фикса:** (опционально) краткий план
-->

_Пока пусто._

---

## ✅ Исправлено

<!-- Переносить сюда с пометкой `[fixed YYYY-MM-DD, commit <hash>]` -->

### [fixed 2026-04-17] Продовые секреты хардкодом в config.py
- **Файл:** `config.py:4,8,12,13`
- **Симптом:** Telegram-токен, Claude API key, email+пароль SberCRM торчали как fallback-значения в `os.getenv(...)`.
- **Что сделано:** создан `.env` (локально и на uz, режим 600), `.env.example` как шаблон, `.gitignore` с исключением `.env`, в `config.py` добавлен `load_dotenv()` + хелпер `_require()` который падает с внятной ошибкой при отсутствии переменной. `python-dotenv>=1.0` добавлен в `requirements.txt` и установлен в venv на uz. Бот перезапущен — polling `@sibur_reestr_bot` работает.
- **Осталось (не блокер):** **ротировать все три секрета**, т.к. они оставались на диске и в process memory неизвестно как долго и могли попасть в бэкапы/скриншоты. До этого безопаснее не публиковать репо.
